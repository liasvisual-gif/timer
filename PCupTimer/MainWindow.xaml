<Window x:Class="PCupTimer.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:local="clr-namespace:PCupTimer"
        mc:Ignorable="d"
        Title="カウントアップタイマー" Height="300" Width="400">
    <Grid x:Name="MainGrid" Background="Blue">
        <Grid.RowDefinitions>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="36*"/>
        </Grid.RowDefinitions>

        <local:OutlinedTextBlock x:Name="TimerDisplay"
                                 Text="00.0"
                                 FontSize="120"
                                 FontWeight="Bold"
                                 Fill="White"
                                 Stroke="Black"
                                 StrokeThickness="4"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" Height="60" Width="240">
            <Button x:Name="StartStopButton"
                    Content="スタート"
                    Width="100"
                    Height="40"
                    FontSize="16"
                    Click="StartStopButton_Click"/>
            <Button x:Name="ResetButton"
                    Content="リセット"
                    Width="100"
                    Height="40"
                    Margin="10"
                    FontSize="16"
                    Click="ResetButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
