<Window x:Class="audition_nagurisaki.OverlayWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="Display" 
WindowStyle="None" 
AllowsTransparency="True" 
Background="Transparent"
Topmost="True"
ShowInTaskbar="False"
Width="400"
Height="300"
ResizeMode="NoResize"
MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid>
        <!-- Context Menu -->
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="閉じる" Click="ContextMenu_Close_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
        
        <!-- Result Text Display Only -->
        <TextBlock x:Name="txtVo" 
                   Text="Vo��Vo" 
                   FontSize="200" 
                   FontWeight="Bold"
                   Foreground="Red"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
        
        <TextBlock x:Name="txtDa" 
                   Text="Da��Da" 
                   FontSize="200" 
                   FontWeight="Bold"
                   Foreground="Blue"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
        
        <TextBlock x:Name="txtVi" 
                   Text="Vi��Vi" 
                   FontSize="200" 
                   FontWeight="Bold"
                   Foreground="Yellow"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Window>

