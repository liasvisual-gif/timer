<Window x:Class="todolist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:todolist"
        mc:Ignorable="d"
        Title="TODOリスト" Height="450" Width="600" Background="#FFEADBCC">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="NewTaskTextBox" Width="300" Height="30" 
                     VerticalContentAlignment="Center" Foreground="Black"
                     KeyDown="NewTaskTextBox_KeyDown"/>
            <DatePicker x:Name="DueDatePicker" Width="120" Height="30" 
                        Margin="10,0,0,0" SelectedDate="{x:Static s:DateTime.Today}"
                        xmlns:s="clr-namespace:System;assembly=mscorlib"/>
            <Button x:Name="AddButton" Content="追加" Width="80" Height="30" 
                    Margin="10,0,0,0" Click="AddButton_Click" 
                    Background="White" Foreground="Black"/>
        </StackPanel>

        <ListBox x:Name="TaskListBox" Grid.Row="1" Background="#FFEADBCC" 
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="内容を変更" Click="EditMenuItem_Click"/>
                            </ContextMenu>
                        </Grid.ContextMenu>
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted, Mode=TwoWay}" 
                                  VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding TaskName}" FontSize="16" 
                                   VerticalAlignment="Center" Foreground="Black">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                            <Setter Property="Foreground" Value="Gray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Grid.Column="2" Text="{Binding DueDateDisplay}" 
                                   FontSize="14" Foreground="DarkGray" 
                                   VerticalAlignment="Center" Margin="10,0,10,0"/>
                        <Button Grid.Column="3" Content="削除" Width="60" Height="25"
                                Click="DeleteButton_Click" Background="White" 
                                Foreground="Red"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
